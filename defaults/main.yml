# Standards: 0.1
---
# @var gitter_user:description: User to run user-specific commands
# @var gitter_user:value: gitter_user | default(homebrew_user) | default(ansible_user_id)
# gitter_user:

# @var gitter_started:description: Restart app if already running
gitter_started: True

...
